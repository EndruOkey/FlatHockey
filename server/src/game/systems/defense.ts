import type { Room } from '../room';
import type { ServerPlayer } from '../entities/player';

export function applyDefense(_room: Room, _player: ServerPlayer) {
  // Defense temporarily disabled due to input protocol mismatch.
}
